Description: dlopen the sonamed libs directly, like it has already been done
  for libGL.so.1, as we don't want to Depend on the -dev packages for the .so files:
  - libosmesa.so.6: was already available in Hardy
  - libGLESv2.so.1 and libEGL.so.1: available only since Maverick

Index: dev.raring/src/ui/gl/gl_implementation_linux.cc
===================================================================
--- dev.raring.orig/src/ui/gl/gl_implementation_linux.cc	2013-05-01 20:23:44.348179574 -0500
+++ dev.raring/src/ui/gl/gl_implementation_linux.cc	2013-05-01 20:23:44.328179013 -0500
@@ -79,7 +79,7 @@
       }
 
       base::NativeLibrary library = LoadLibrary(
-          module_path.Append("libosmesa.so"));
+          module_path.Append("libosmesa.so.6"));
       if (!library)
         return false;
 
