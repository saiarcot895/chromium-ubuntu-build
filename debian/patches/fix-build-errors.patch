Index: dev/ui/views/view.h
===================================================================
--- dev.orig/ui/views/view.h
+++ dev/ui/views/view.h
@@ -530,7 +530,7 @@ class VIEWS_EXPORT View : public ui::Lay
     SetLayoutManagerImpl(std::move(layout_manager));
     return lm;
   }
-  void SetLayoutManager(nullptr_t);
+  void SetLayoutManager(std::nullptr_t);
   // DEPRECATED version that takes ownership of a raw pointer.
   void SetLayoutManager(LayoutManager* layout);
 
Index: dev/ui/views/view.cc
===================================================================
--- dev.orig/ui/views/view.cc
+++ dev/ui/views/view.cc
@@ -631,7 +631,7 @@ LayoutManager* View::GetLayoutManager()
   return layout_manager_.get();
 }
 
-void View::SetLayoutManager(nullptr_t) {
+void View::SetLayoutManager(std::nullptr_t) {
   SetLayoutManagerImpl(nullptr);
 }
 
