Index: dev.trusty/chrome/test/chromedriver/embed_version_in_cpp.py
===================================================================
--- dev.trusty.orig/chrome/test/chromedriver/embed_version_in_cpp.py	2014-05-07 19:27:20.355310011 -0500
+++ dev.trusty/chrome/test/chromedriver/embed_version_in_cpp.py	2014-05-07 19:27:20.351310011 -0500
@@ -25,7 +25,9 @@
   options, args = parser.parse_args()
 
   version = open(options.version_file, 'r').read().strip()
-  revision = lastchange.FetchVersionInfo(None).revision
+  lastchangeFile = open(os.path.join(chrome_paths.GetSrc(), 'build', 'util', 'LASTCHANGE'), 'r')
+  lastchangeFile.seek(11)
+  revision = lastchangeFile.read()
   if revision:
     version += '.' + revision.strip()
 
