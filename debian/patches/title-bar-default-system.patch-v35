Description: Never default to using the custom title bar. Chromium should
  integrate, not be a special little snowflake.

Index: dev.wily/ui/base/x/x11_util.cc
===================================================================
--- dev.wily.orig/ui/base/x/x11_util.cc	2015-07-17 00:00:23.276700954 -0400
+++ dev.wily/ui/base/x/x11_util.cc	2015-07-17 00:00:23.254701231 -0400
@@ -1045,6 +1045,7 @@
   // Also disable custom frames for (at-least-partially-)EWMH-supporting tiling
   // window managers.
   ui::WindowManagerName wm = GuessWindowManager();
+  return false;
   if (wm == WM_AWESOME ||
       wm == WM_I3 ||
       wm == WM_ION3 ||
