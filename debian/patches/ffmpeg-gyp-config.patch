Bug-Chromium: https://code.google.com/p/chromium/issues/detail?id=159349
                                                                       
Index: dev.trusty/src/third_party/ffmpeg/ffmpeg.gyp
===================================================================
--- dev.trusty.orig/src/third_party/ffmpeg/ffmpeg.gyp	2013-12-13 22:17:26.805042357 -0600
+++ dev.trusty/src/third_party/ffmpeg/ffmpeg.gyp	2013-12-13 22:17:26.733039792 -0600
@@ -132,6 +132,10 @@
             ],
             'yasm_output_path': '<(shared_generated_dir)/yasm'
           },
+          'include_dirs': [
+            '<(platform_config_root)',
+            '.',
+          ],
         },
       ] # targets
     }], # arch != arm
@@ -501,6 +505,9 @@
   'targets': [
     {
       'target_name': 'ffmpeg',
+      'include_dirs': [
+        '<(platform_config_root)',
+      ],
       'sources': [
         # Files needed for stub generation rules.
         '<@(sig_files)',
@@ -588,6 +595,7 @@
           ],
           'type': '<(component)',
           'include_dirs': [
+	    '<(platform_config_root)',
             '<(output_root)',
             '../..',  # The chromium 'src' directory.
             '<(platform_config_root)',
Index: dev.trusty/src/third_party/icu/icu.gyp
===================================================================
--- dev.trusty.orig/src/third_party/icu/icu.gyp	2013-12-13 22:17:26.805042357 -0600
+++ dev.trusty/src/third_party/icu/icu.gyp	2013-12-13 22:17:26.793041929 -0600
@@ -31,8 +31,11 @@
          target_arch=="mipsel")', {
         'target_conditions': [
           ['_toolset=="host"', {
-            'cflags': [ '-m32' ],
-            'ldflags': [ '-m32' ],
+            'variables': {
+              'm32flag': '<!((echo | $(echo ${CXX_host:-$(which g++)}) -m32 -E - > /dev/null 2>&1) && echo "-m32" || true)',
+            },
+            'cflags': [ '<(m32flag)' ],
+            'ldflags': [ '<(m32flag)' ],
             'asflags': [ '-32' ],
             'xcode_settings': {
               'ARCHS': [ 'i386' ],
