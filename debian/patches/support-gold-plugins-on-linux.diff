Index: dev/build/config/compiler/BUILD.gn
===================================================================
--- dev.orig/build/config/compiler/BUILD.gn
+++ dev/build/config/compiler/BUILD.gn
@@ -564,12 +564,12 @@ config("compiler") {
   # TODO(pcc): Make this conditional on is_official_build rather than on gn
   # flags for specific features.
   if (!is_debug && use_thin_lto && is_a_target_toolchain) {
-    assert(use_lld || target_os == "chromeos",
-           "gold plugin only supported with ChromeOS")
+    assert(use_lld || target_os == "chromeos" || target_os == "linux",
+           "gold plugin only supported with ChromeOS and Linux")
 
     cflags += [ "-flto=thin" ]
 
-    if (target_os != "chromeos") {
+    if (target_os != "chromeos" && target_os != "linux") {
       cflags += [ "-fsplit-lto-unit" ]
     }
 
